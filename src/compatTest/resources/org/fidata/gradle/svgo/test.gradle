plugins {
  id 'org.fidata.svgo'
}
svgo {

}
task(type: Svgo, 'svgo1') {
  input = file('input.svg')
  output = file('output.svg')
  config = [
    plugins: [
      removeAttrs: [
        attrs: '(font-size|font-family|letter-spacing|word-spacing|style)'
      ]
    ]
  ]
}
